
// Stub file created for build compatibility
import Foundation

// Basic RateLimiter class to satisfy the compiler
class RateLimiter {
    static let shared = RateLimiter()
    
    func shouldProcess(key: String) -> Bool {
        return true
    }
}
